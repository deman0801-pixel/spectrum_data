останется нерешенной проблема
переполнения памяти в множестве посещенных
ссылок visited (не успеваю).
Предположительно решал бы:
1) установил лимит ссылок
2) при заполнении извлекал
30%(тут бы еще подумал про извлечении редко используемых)
и переносил в SQLite (там надо индексы навесить)
3) проверка ссылки в visited затем в БД

Логирование тоже уже сделать не успеваю. Оставил принтами


для запуска нужно создать .env. 
Предлагаю просто скопировать и переименовать .env.example 
(в качестве хоста бд уже подставлено имя сервиса)

docker-compose up --build (-d запустит процесс в фоне)
приложение будет на 8000 порту.

Постгрес тоже решил прокинуть наружу, для просмотра через UI. 
Доступно на 8100